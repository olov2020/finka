import {StyleSheet} from 'react-native';

export const safeAreaViewStyle = StyleSheet.create({
    safeAreaView: {
        flex: 1,
        gap: 12,
        width: '100%',
        height: '100%',
        alignItems: 'center',
        padding: 8,
    }
});
