import {StyleSheet} from 'react-native';

export const themedViewStyle = StyleSheet.create({
    themedView: {
        flex: 1,
        width: '100%',
        height: '100%',
        alignItems: 'center',
    }
});
